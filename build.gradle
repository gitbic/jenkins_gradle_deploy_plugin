plugins {
    id 'java-gradle-plugin'
    id 'maven-publish'
    id 'java'
}

group 'ru.clevertec'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

apply plugin: 'groovy'

gradlePlugin {
    plugins {
        jacocoPlugin {
            id = 'jacoco-plugin'
            implementationClass = 'ru.clevertec.plugin.jacoco.JacocoPlugin'
        }
        sonarPlugin {
            id = 'sonar-plugin'
            implementationClass = 'ru.clevertec.plugin.sonarqube.SonarPlugin'
        }
        dockerPlugin {
            id = 'docker-plugin'
            implementationClass = 'ru.clevertec.plugin.docker.DockerPlugin'
        }
        ApplyPlugin {
            id = 'jenkins-gradle-deploy-plugin'
            implementationClass = 'ru.clevertec.plugin.GeneralPlugin'
        }
    }
}



dependencies {
    runtimeOnly 'org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:3.3'
    runtimeOnly 'com.bmuschko:gradle-docker-plugin:6.7.0'
    testImplementation group: 'junit', name: 'junit', version: '4.13.2'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'ru.clevertec'
            artifactId = 'jenkins-gradle-deploy-plugin'
            version = '1.0'
        }
    }
}


//buildscript {
//    repositories {
//        mavenLocal()
//    }
//    dependencies {
//        classpath 'ru.clevertec:jenkins-gradle-plugin:1.0-SNAPSHOT'
//    }
//}
//
//apply plugin: 'apply-plugin'
